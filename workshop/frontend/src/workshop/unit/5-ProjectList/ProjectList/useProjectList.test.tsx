describe('useProjectList', () => {
    // Hint: Try to seperate ProjectList into 2 functions
    // 1. useProjectList
    // 2. ProjectList that use useProjectList
    // Then test each functions individually
})

export {}