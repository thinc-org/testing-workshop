// thinc-org/cugetreg-frontend
import * as ORIGINAL_ENV from './environments'

describe('sendCollectedLog', () => {
        // Reminder: Since this is unit tets, you have to mock ./httpClient and ./environments with jest
        it.todo('Write test when data length is 0')
        it.todo(`Write test when NODE_ENV is development`)
        it.todo('Write test for Axios call')
        it.todo(`Write test for Axios call and error`)
})

export { }